# Python/test output
__pycache__/
output/
.venv/
.pytest*/
.env
.soak/

# Arduino output and local settings
build/
temp/
.vscode/
arduino_secrets.h
*.bat
# No blanket *.txt rule: the .txt files worth ignoring all live under directories
# ignored above or under tools/cache/ below, while keywords.txt, the license texts
# and the Classic host's CMakeLists.txt are files the release gates require. A
# blanket rule hid the latter two until CI failed on a clean checkout.

# Release helper cache
tools/cache/
