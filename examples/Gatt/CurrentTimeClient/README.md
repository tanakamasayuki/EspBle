# Gatt/CurrentTimeClient

Connects to a peripheral advertising the standard Current Time Service
(`0x1805`), reads and decodes its 10-byte Current Time value, and subscribes to
notifications.

Use it with [CurrentTimeServer](../CurrentTimeServer/).
