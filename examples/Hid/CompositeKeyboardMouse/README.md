# Composite BLE HID Keyboard + Mouse

Calling both `hidKeyboard().configure()` and `hidMouse().configure()` before `begin()` composes one HID Service with fixed Report IDs 1 and 2.

> 日本語版: [README.ja.md](README.ja.md)
